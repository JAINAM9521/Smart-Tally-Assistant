"use client";
import { Check } from "lucide-react";
import { ScoreRing } from "./LandingPrimitives";

export default function ValidationSection({ validationRef, validationInView }) { return <section className="section validation-section" id="validation" ref={validationRef}><div className="section-heading"><div><div className="eyebrow">THE USP</div><h2>Don&apos;t just find errors.<br /><em>Understand them.</em></h2></div><p>Before XML generation, the validation engine checks the details that matter in accounting: structure, ledgers, GST, duplicates and balance.</p></div><div className="validation-layout"><div className="metric-panel"><div className="metric-top"><span>Validation score</span><strong>98.6%</strong></div><ScoreRing active={validationInView} /><div className="mini-metrics"><span><b>1,250</b>Rows checked</span><span><b className="green">1,245</b>Valid rows</span><span><b className="red">5</b>Errors</span><span><b className="amber">8</b>Warnings</span></div></div><div className="check-list"><h3>Checks that protect your books</h3>{["Required fields & date formats", "Ledger names & voucher types", "GSTIN, PAN & HSN/SAC", "Duplicate vouchers & invoices", "Debit / credit balance", "Tally XML compatibility"].map((x, i) => <div className="check-row" key={x}><span><Check size={14} /></span>{x}<small>{i < 4 ? "Passed" : "Ready"}</small></div>)}</div></div></section> }
