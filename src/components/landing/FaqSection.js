"use client";
import { ChevronDown } from "lucide-react";

const faqs = ["What is Smart Tally XML Assistant?", "Which file formats are supported?", "What does validation check?", "Can common errors be fixed automatically?", "Can I preview XML before downloading?", "How do I import XML into Tally?"];
export default function FaqSection({ open, setOpen }) { return <section className="section faq" id="faq"><div className="section-heading"><div><div className="eyebrow">NEED TO KNOW</div><h2>Answers before<br /><em>you begin.</em></h2></div><p>A safer conversion starts with clear expectations. Here are the common questions from finance teams.</p></div><div className="faq-list">{faqs.map((x, i) => <button key={x} onClick={() => setOpen(open === i ? null : i)}><span><b>0{i + 1}</b>{x}</span><ChevronDown className={open === i ? "rotate" : ""} />{open === i && <p>{i === 0 ? "Smart Tally XML Assistant is a frontend workspace that prepares, validates and simulates Tally-compatible XML from accounting spreadsheets." : "Yes. The demo supports spreadsheet-friendly CSV, XLS and XLSX workflows, with realistic mock data for showcasing the complete product journey."}</p>}</button>)}</div></section> }
