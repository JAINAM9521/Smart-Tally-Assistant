const Upload = require("../models/Upload"); const Validation = require("../models/Validation"); const XMLFile = require("../models/XMLFile");
exports.dashboard = async user => { const scope = user.role === "admin" ? {} : { user: user._id }; const [totalUploads, totalValidations, totalXMLFiles, uploads] = await Promise.all([Upload.countDocuments(scope), Validation.countDocuments(scope), XMLFile.countDocuments(scope), Upload.find(scope).select("rows voucherType")]); const totalRows = uploads.reduce((sum, item) => sum + (item.rows || 0), 0); return { totalUploads, totalRows, totalTransactions: totalRows, totalValidations, totalXMLFiles, successRate: totalValidations ? Math.round((await Validation.countDocuments({ ...scope, status: "validated" }) / totalValidations) * 100) : 0, errorRate: 0, averageProcessingTime: 0, autoFixRate: 0, voucherBreakdown: uploads.reduce((out, item) => { out[item.voucherType] = (out[item.voucherType] || 0) + 1; return out; }, {}) }; };
