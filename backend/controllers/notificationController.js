const Notification = require("../models/Notification"); exports.list = async (req, res, next) => { try { const items = await Notification.find({ user: req.user._id }).sort("-createdAt").limit(50); res.json({ success: true, items }); } catch (e) { next(e); } }; exports.read = async (req, res, next) => { try { const item = await Notification.findOneAndUpdate({ _id: req.params.id, user: req.user._id }, { read: true }, { new: true }); res.json({ success: true, item }); } catch (e) { next(e); } };
