const express = require("express"); const cors = require("cors"); const helmet = require("helmet"); const { clientUrl } = require("./config/env"); const { notFound, errorHandler } = require("./middleware/errorMiddleware");
const app = express(); app.use(helmet()); app.use(cors({ origin: clientUrl, credentials: true })); app.use(express.json({ limit: "2mb" }));
app.get("/api/health", (req, res) => res.json({ success: true, message: "Smart Tally API is running", timestamp: new Date().toISOString() }));
app.use("/api/auth", require("./routes/authRoutes")); app.use("/api/user", require("./routes/userRoutes")); app.use("/api/uploads", require("./routes/uploadRoutes")); app.use("/api/templates", require("./routes/templateRoutes")); app.use("/api/conversions", require("./routes/conversionRoutes")); app.use("/api/validation", require("./routes/validationRoutes")); app.use("/api/recommendations", require("./routes/recommendationRoutes")); app.use("/api/xml", require("./routes/xmlRoutes")); app.use("/api/reports", require("./routes/reportRoutes")); app.use("/api/analytics", require("./routes/analyticsRoutes")); app.use("/api/notifications", require("./routes/notificationRoutes")); app.use("/api/chatbot", require("./routes/chatbotRoutes"));
app.use(notFound); app.use(errorHandler); module.exports = app;
