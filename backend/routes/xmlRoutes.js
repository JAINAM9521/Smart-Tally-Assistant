const router = require("express").Router(); const c = require("../controllers/xmlController"); const { requireAuth } = require("../middleware/authMiddleware"); const { allowRoles } = require("../middleware/roleMiddleware"); router.get("/", requireAuth, c.list); router.post("/generate/:validationId", requireAuth, allowRoles("admin", "accountant"), c.generate); router.get("/:xmlId/download", requireAuth, c.download); router.get("/:xmlId", requireAuth, c.get); router.delete("/:xmlId", requireAuth, allowRoles("admin", "accountant"), c.remove); module.exports = router;
