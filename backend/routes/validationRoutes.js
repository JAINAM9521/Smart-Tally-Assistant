const router = require("express").Router(); const c = require("../controllers/validationController"); const { requireAuth } = require("../middleware/authMiddleware"); const { allowRoles } = require("../middleware/roleMiddleware"); router.post("/:uploadId/validate", requireAuth, allowRoles("admin", "accountant"), c.validate); router.get("/:validationId", requireAuth, c.get); router.post("/:validationId/auto-fix", requireAuth, allowRoles("admin", "accountant"), c.autoFix); router.post("/:validationId/revalidate", requireAuth, allowRoles("admin", "accountant"), c.revalidate); router.post("/:validationId/issues/:issueId/fix", requireAuth, allowRoles("admin", "accountant"), c.fixIssue); router.post("/:validationId/issues/:issueId/apply", requireAuth, allowRoles("admin", "accountant"), c.apply); router.post("/:validationId/issues/:issueId/ignore", requireAuth, allowRoles("admin", "accountant"), c.ignore); module.exports = router;
